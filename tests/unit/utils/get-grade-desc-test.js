import getGradeDesc from 'nutri-track/utils/get-grade-desc';
import { module, test } from 'qunit';

module('Unit | Utility | get-grade-desc', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getGradeDesc();
    assert.ok(result);
  });
});
