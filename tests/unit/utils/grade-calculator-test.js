import gradeCalculator from 'nutri-track/utils/grade-calculator';
import { module, test } from 'qunit';

module('Unit | Utility | grade-calculator', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = gradeCalculator();
    assert.ok(result);
  });
});
