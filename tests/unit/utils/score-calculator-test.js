import scoreCalculator from 'nutri-track/utils/score-calculator';
import { module, test } from 'qunit';

module('Unit | Utility | score-calculator', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = scoreCalculator();
    assert.ok(result);
  });
});
