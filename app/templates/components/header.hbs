<nav class="navbar navbar-light bg-light static-top">
  <div class="container">
    {{#link-to "index" class="navbar-brand"}}Nutri-Track{{/link-to}}
    <div class="text-right">
      {{#if session.isAuthenticated}}
        {{#link-to "my-foods" class="ml-2"}}My Foods{{/link-to}}
        <a href="#" class="ml-2" {{action "invalidateSession"}}>Logout</a>
      {{else}}
        {{#link-to "register" class="btn btn-primary ml-2"}}Register{{/link-to}}
        {{#link-to "login" class="btn btn-primary ml-2"}}Login{{/link-to}}
      {{/if}}
    </div>
  </div>
</nav>
